namespace Library.Models
{
    /// <summary>
    /// Base class to enable generic interface classes for front end models
    /// Violates dependancy inversion and should be checked when used
    /// </summary>
    public class BaseModel
    {
    }
}
